# Frontend — Deployments Dashboard

Next.js 14 App Router + TypeScript (strict) + Tailwind CSS + TanStack Query v5.

## Setup

```bash
cd frontend
npm install
cp .env.local.example .env.local
```

## Run

```bash
npm run dev
# http://localhost:3000
```

## Build check

```bash
npm run build
```
